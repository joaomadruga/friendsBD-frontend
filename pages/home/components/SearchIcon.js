import styled from "styled-components";

const SearchIcon = styled.div`
    display: flex;
    position: absolute;
    padding-top: 15px;
    padding-left: 12px;
    z-index: 0;
`;

export default SearchIcon